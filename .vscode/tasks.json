{
    // This tasks.json file is configured for Visual Studio Code using MSVC.
    // It now includes the Vulkan SDK by adding include and library paths,
    // and links against the Vulkan library (vulkan-1).
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Run compile.bat",
            "command": "compile.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}